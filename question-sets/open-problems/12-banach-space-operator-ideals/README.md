# Banach Space Theory and Operator Ideals

This category contains the imported operator-ideal problems 1.6--1.8 and the Section 2 Banach-space problems 2.1--2.11 from the supplied PDF transcription.

Each entry preserves its source-local number in `source.locator`. The supplied transcription removed bibliographic source metadata and status, so all entries are initially recorded as `raw` pending source verification.
