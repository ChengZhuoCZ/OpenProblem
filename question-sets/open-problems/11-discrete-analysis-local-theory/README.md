# Discrete Analysis, Local Theory, and Quantum Boolean Functions

This category contains the imported Section 1 problems (1.1--1.10) and the related tail-space and endpoint Riesz problems labelled 2.12--2.13 in the supplied PDF transcription.

Each entry preserves its source-local number in `source.locator`. The supplied transcription removed bibliographic source metadata and status, so all entries are initially recorded as `raw` pending source verification.
