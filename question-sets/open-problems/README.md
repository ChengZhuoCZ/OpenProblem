# Open Problems

This collection stores research-style mathematical open problems.

Each problem is maintained as a directory with two required files:

- `problem.md`: the problem statement, background, theorem form, and source notes.
- `progress.md`: the current progress record, open questions, next actions, and work log.

Use `index.md` as the canonical entry point for browsing problems.
